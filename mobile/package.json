{
  "name": "spvn-cet-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Android Capacitor shell for the SPVN CET Portal",
  "scripts": {
    "sync": "npx cap sync android",
    "android": "npx cap open android",
    "assets": "npx @capacitor/assets generate --android",
    "apk:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.4.3",
    "typescript": "~6.0.3"
  }
}
